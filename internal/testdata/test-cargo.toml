[package]
name = "my-rust-app"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
tokio = { version = "1.7.0", features = ["full"] }
hyper = "0.14.5"
regex = "1.5.4"
clap = "4.0.0"
log = "0.4"
env_logger = "0.10"

[dev-dependencies]
criterion = "0.5"
proptest = "1.0"
